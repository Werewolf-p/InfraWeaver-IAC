apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base manifests for infraweaver-node — environment-agnostic.
# Tunable params (image tag, scaling) live in overlays/prod/kustomization.yaml.
resources:
  - deployment.yaml
  - externalsecret-ghcr.yaml
  - rbac.yaml
  - secret-template.yaml
  - serviceaccount.yaml

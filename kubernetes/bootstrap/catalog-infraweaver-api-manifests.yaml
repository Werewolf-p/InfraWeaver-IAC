---
# catalog-infraweaver-api-manifests.yaml — Auto-generated by scripts/sync-catalog.sh
# Source: kubernetes/catalog/infraweaver-api/overlays/prod/
# DO NOT edit manually — changes will be overwritten on next sync.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: catalog-infraweaver-api-manifests
  namespace: argocd
  labels:
    infraweaver.io/type: catalog-app
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: platform
  source:
    repoURL: ${DEPLOY_REPO_URL}
    targetRevision: main
    path: kubernetes/catalog/infraweaver-api/overlays/prod
  destination:
    server: https://kubernetes.default.svc
    namespace: "infraweaver-console"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
  ignoreDifferences:
    - group: external-secrets.io
      kind: ExternalSecret
      jsonPointers:
        - /status
        - /metadata/finalizers
        - /spec/target/creationPolicy
        - /spec/target/deletionPolicy
      jqPathExpressions:
        - '.spec.data[].remoteRef.conversionStrategy'
        - '.spec.data[].remoteRef.decodingStrategy'
        - '.spec.data[].remoteRef.metadataPolicy'
    - group: apps
      kind: ReplicaSet
      jsonPointers:
        - /spec/replicas
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
